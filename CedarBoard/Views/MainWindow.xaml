<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="CedarBoard.Views.MainWindow"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="700" Width="1050" 
        WindowStartupLocation="CenterScreen">
    <Grid>
        <ui:TextBlock HorizontalAlignment="Left" Margin="70,29,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="90" Width="480">
            <ui:TextBlock.Effect>
                <DropShadowEffect Color="Gray" Direction="315" ShadowDepth="9" BlurRadius="10" />
            </ui:TextBlock.Effect>
            <Run Language="ja-jp" Text="CedarBoard" FontSize="60" Foreground="LimeGreen"/>
        </ui:TextBlock>

        <ui:Button HorizontalAlignment="Left" Margin="715,223,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.5,0.5" Width="300" Height="43"
                   Content="新規作成" FontSize="20" Background="White" >
            <ui:Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}"
                    BorderBrush="Black"
                    BorderThickness="3"
                    CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </ui:Button.Template>
        </ui:Button>

        <ui:Button HorizontalAlignment="Left" Margin="715,306,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.5,0.5" Width="300" Height="39"
                   Content="選択した作品を編集" FontSize="20" Background="White" >
            <ui:Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}"
                    BorderBrush="Black"
                    BorderThickness="3"
                    CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </ui:Button.Template>
        </ui:Button>

        <ui:Button HorizontalAlignment="Left" Margin="715,385,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.5,0.5" Width="300" Height="42"
                   Content="選択した作品を削除" FontSize="20" Background="Red" >
            <ui:Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}"
                    BorderBrush="Black"
                    BorderThickness="3"
                    CornerRadius="10">
                        <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </ui:Button.Template>
        </ui:Button>
        
        <ui:ListView av:ItemsSource="{av:SampleData ItemCount=5}" Margin="5,219,365,80">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Path=Name}" Margin="5" />
                        <TextBlock Text="{Binding Path=Age}" Margin="5" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ui:ListView>
    </Grid>
</Window>